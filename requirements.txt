# ============================================================
# 胚胎多模态活产概率预测 —— 一键安装依赖
# 用法：pip install -r requirements.txt
#
# 说明：
#   - Linux 下 torch 默认装 CUDA 版；无 GPU 请先装 CPU 版：
#     pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   - 版本均为下限约束，按需可自行调整。
# ============================================================

# ---------- 表格建模（必需，全部脚本共用） ----------
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
catboost>=1.2
joblib>=1.3

# ---------- 视频建模（需 GPU，03/04/05 video 模式使用） ----------
Pillow>=10.0
torch>=2.1
torchvision>=0.16
transformers>=4.40
